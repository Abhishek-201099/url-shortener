export async function getShortURL(url) {
  //  FIX THIS BUG
  // try {
  //   const response = await fetch(`https://cdpt.in/shorten?url=${url}`);
  //   if (!response.ok) {
  //     throw new Error("Request not fullfilld");
  //   }
  //   const data = await response.text();
  //   return data;
  // } catch (error) {
  //   console.log("Error : ", error);
  // }
}
